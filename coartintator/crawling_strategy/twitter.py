from coartintator.crawling_strategy import AbstractCrawlingStrategy


class TwitterCrawlingStrategy(AbstractCrawlingStrategy):
    def crawl(self, feed):
        # logic to crawl a Twitter feed
        pass
